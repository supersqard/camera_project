spring:
  datasource:
    url: jdbc:mysql://localhost:3306/camera
    username: root
    password: p410910
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
    properties:
      hibernate:
        format_sql: true